PRIMA-NEXT PHASE CONTRACT

SCOPE
- Implement only the requested phase; never continue automatically.
- The phase prompt sets scope; this file and C:\PRIMA_integrated\Draft.png are architectural ground truth/default fallback. Never exceed either.
- Before edits: read this file, README.md, pyproject.toml, and the prior phase report; inspect git status/diff; verify prior acceptance criteria.
- Stop on conflict, ambiguity, missing prerequisites, incomplete prior work, or unexplained changes. Report the decision, evidence, blocker, impact, and safe options; await review.

ARCHITECTURE
One public runtime boundary -> typed request -> task/profile routing -> one workflow-owned lifecycle -> selected subsystems -> typed result + diagnostics.

Tasks: conversation, factual_qa, document_ingestion, emotion_classification, tool_request.
Profiles: model_only, simple_rag, prima_full, affect_only, ingestion_only.
Profiles are configuration, never benchmark-specific production imports. All tasks use the same boundary; routes may shorten, never become hidden pipelines. Diagnostics must name every activated/skipped subsystem and prove calls.

Canonical factual/conversation state machine (bounded):
parse -> load state -> affect -> retrieve -> plan -> simulate world -> decide uncertainty -> bounded reflect/retrieve/replan -> LLM/tool execute -> validate output -> bounded correction -> shape result -> commit state/memory -> emit maintenance events.
Ingestion and emotion classification use explicit shorter routes; never force irrelevant QA stages.

ENGINEERING
- Workflow owns orchestration; event bus owns async communication; ChromaDB is memory source of truth.
- No independent QA/classification/ingestion pipelines or direct subsystem coupling.
- Thin compatibility adapters are allowed; defective architecture is not preserved.
- Python 3.10; strict typing; documented public APIs/types; dataclasses/Pydantic; dependency injection; async-compatible; security-first; no circular dependencies.
- No global mutable config, singletons/model clients, hidden environment mutation, blanket exceptions/type ignores, silent fallbacks, skipped tests, or weakened assertions.
- Never expose gold answers/labels/supporting facts/evidence IDs/evaluation results to prompts, retrieval, routing, or reflection.
- Never modify benchmark gold data or historical results. Production must not import benchmarks.
- Do not hard-code model/GPU/endpoint/credentials or create heavyweight model instances per benchmark worker.
- Do not download models or run full datasets unless explicitly required.
- Every behavior needs focused tests; every JSON artifact needs a schema version.
- A class existing does not make a component active: tests + diagnostics must prove execution.
- Never claim the trained TF-IDF/logistic classifier proves PRIMA improved Qwen.
- Never call HotpotQA supplied context open-domain Wikipedia retrieval.
- Prefer the smallest real implementation; preserve valid benchmark/affect/memory/reflection behavior, not broken internals.

EXECUTION
1. Inspect affected code and callers.
2. Add/update failing characterization tests when practical.
3. Make the smallest coherent change.
4. Run focused/full tests, compileall, Ruff, mypy, Bandit, and Xenon via scripts/run_xenon.py when available; record exact results. Never run Xenon on repository root.

HANDOFF
- List changed files, contract/behavior changes, exact commands/results, risks, and deferrals.
- Create docs/implementation/phase_<NN>_<name>.md and update docs/implementation/STATUS.md.
- Commit all phase changes with a detailed message; never push unless explicitly requested.
- Stop.
