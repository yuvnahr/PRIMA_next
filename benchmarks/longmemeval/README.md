# LongMemEval

Placeholder for a future LongMemEval benchmark integration.

Future code should implement the shared interfaces from `benchmarks.common` and adapt raw LongMemEval
records into benchmark-neutral `Conversation` objects.
