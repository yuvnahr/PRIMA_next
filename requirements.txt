# Default contributor/CI environment. Optional metrics and encoder training are
# intentionally excluded; install their requirement files only when needed.
-r requirements-core.txt
-r requirements-benchmark.txt
-r requirements-dev.txt
